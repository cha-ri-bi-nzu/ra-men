<h1>⚔️福岡&横浜2大豚骨ラーメンバトルブログ⚔️</h1>

<table>
  <tr>
    <th>本日の麺</th>
    <th>評価</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.title %></td>
    <td><%= blog.content %></td>
    <td><%= link_to '編集' , edit_blog_path(blog.id) %></td>
    <td><%= link_to '確認' , blog_path(blog.id) %></td>
    <td><%= link_to '抹消' , blog_path(blog.id), method: :delete %></td>
  </tr>
<% end %>
</table>

<br>
<%= link_to '本日の🍜', new_blog_path %>
